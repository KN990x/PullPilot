# Secrets and local config — MUST NEVER be committed (they hold AUTH_PASS / SESSION_SECRET)
.env
.env.*
!.env.example
*.env

# Python
# .python-version IS tracked on purpose: it pins the project's pyenv version.
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/

# Node / frontend
node_modules/
web/dist/
web/build/
web/coverage/
.npm/
.pnpm-store/
.pnpm-debug.log*
# npm is no longer used: the project lockfile is web/pnpm-lock.yaml.
package-lock.json
yarn.lock

# Runtime data
data/
server/data/
# DATA_DIR de `make dev-server`: base de datos y secreto de sesión locales.
.devdata/
*.db
*.sqlite
*.sqlite3
pullpilot.db
session_secret.key

# Local agent instructions: not part of the project
AGENTS.md
CLAUDE.md

# Editors and OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
