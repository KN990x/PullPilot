# Secrets and local config — MUST NEVER be committed
.env
.env.*
!.env.example
*.env

# Python
# .python-version IS tracked on purpose: it pins the project's pyenv version.
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/

# Node / frontend
node_modules/
web/dist/
# Vite bundle inside the package: produced by the Docker build, or by copying web/dist
# here to exercise the 404 -> SPA fallback locally.
server/static/
web/build/
web/coverage/
.npm/
.pnpm-store/
.pnpm-debug.log*
# npm is no longer used: the project lockfile is web/pnpm-lock.yaml.
package-lock.json
yarn.lock

# Runtime data
data/
server/data/
# DATA_DIR for `make dev-server`: local database and session secret.
.devdata/
*.db
*.sqlite
*.sqlite3
pullpilot.db
session_secret.key

# Local agent instructions, not part of the project
AGENTS.md
CLAUDE.md

# Editors and OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
